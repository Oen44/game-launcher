import { hot } from 'react-hot-loader/root';
import * as React from 'react';

require('./LoginWindow.scss');

const LoginWindow = () => <div>Hello World from Electron!</div>;

export default hot(LoginWindow);
